# 🐍 Snake Game (Python + Turtle)

A classic **Snake Game** built using **Python** and the **`turtle`** module.

Control the snake with arrow keys, eat food to grow longer, and avoid colliding with the walls or yourself!

---

## 🎮 How to Play

1. Clone the repository.
2. Run the game using Python.
3. Use the **arrow keys** to control the snake:
   - ⬆️ Up
   - ⬇️ Down
   - ⬅️ Left
   - ➡️ Right
4. Try to eat the food to grow longer.
5. **Avoid hitting the walls or your own tail** — that ends the game!

---

## 🧠 Project Structure

This project includes multiple modules:

| File          | Purpose |
|---------------|---------|
| `main.py`     | Entry point — runs the game loop |
| `snake.py`    | Snake movement & body logic |
| `food.py`     | Food spawning logic |
| `score.py`    | Score tracking and display |

---

## 📦 Requirements

Make sure you have **Python 3.x** installed.  
No external libraries are required — the game uses only Python’s built-in `turtle` module.

---

## 🏁 How to Run

1. Clone the repository:
   ```bash
   git clone https://github.com/sahaasmita60-ship-it/Snake-Game-Turtle.git
exit
2.Run the game:
 ```bash
python main.py

